management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      enabled: true
    info:
      enabled: true

#logging:
#  level:
#    org.springframework: DEBUG  
    