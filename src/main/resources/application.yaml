server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "*"
    prometheus: 
      enabled: true
  endpoint:
    health:
      enabled: true
    info:
      enabled: true

#logging:
#  level:
#    org.springframework: DEBUG  
    