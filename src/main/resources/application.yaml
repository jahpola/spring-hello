server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true

logging:
 level:
   org.springframework: DEBUG
